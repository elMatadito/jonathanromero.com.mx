import Header from "../Header";
import Jobs from "../Jobs";
import { Main } from "./Layout.styled";

const Layout = () => {
  return (
    <Main>
      <Header />
      {/* <Jobs /> */}
    </Main>
  )
};

export default Layout;
